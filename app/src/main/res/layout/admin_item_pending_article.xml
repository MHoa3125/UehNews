<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Tiêu đề bài viết -->
        <TextView
            android:id="@+id/tv_article_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/ueh_blue_dark"
            android:textSize="18sp"
            android:textStyle="bold"
            tools:text="Đây là một tiêu đề bài viết rất dài để kiểm tra hiển thị trên nhiều dòng" />

        <!-- Tác giả -->
        <TextView
            android:id="@+id/tv_article_author"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:drawablePadding="8dp"
            android:gravity="center_vertical"
            android:textSize="14sp"
            app:drawableTint="@color/ueh_blue"
            tools:text="Tác giả: Nguyễn Văn A" />

        <!-- Ngày gửi -->
        <TextView
            android:id="@+id/tv_article_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:drawablePadding="8dp"
            android:gravity="center_vertical"
            android:textColor="@color/material_on_surface_disabled"
            android:textSize="12sp"
            app:drawableTint="@color/material_on_surface_disabled"
            tools:text="Ngày gửi: 25/12/2025" />

        <!-- Dải phân cách -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="16dp"
            android:background="?android:attr/listDivider" />

        <!-- Nút hành động -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:orientation="horizontal">

            <!-- Nút Từ chối (màu cam, kiểu outlined) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_reject"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:layout_weight="1"
                android:text="Từ chối"
                android:textColor="@color/ueh_orange"
                app:strokeColor="@color/ueh_orange" />

            <!-- Nút Duyệt (màu xanh, kiểu filled) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_approve"
                style="@style/Widget.MaterialComponents.Button"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:backgroundTint="@color/ueh_blue"
                android:text="Duyệt" />
        </LinearLayout>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
