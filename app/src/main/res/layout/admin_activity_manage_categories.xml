<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ueh_light_gray"
    tools:context=".ui.admin.ManageCategoriesActivity">

    <!-- 1. Thanh tiêu đề (App Bar) với màu xanh UEH -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Toolbar được thiết kế lại, nhưng vẫn dùng ID cũ để tương thích -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/tv_header_title"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/ueh_blue"
            app:title="Quản Lý Danh Mục"
            app:titleTextColor="@color/ueh_white" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- 2. Vùng chứa nội dung chính -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- Danh sách danh mục (ID được giữ nguyên) -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_categories"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:padding="8dp"
            tools:listitem="@layout/admin_item_category" />

        <!-- ProgressBar (ID được giữ nguyên) -->
        <ProgressBar
            android:id="@+id/progress_bar_categories"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:visibility="gone"
            tools:visibility="visible" />

    </RelativeLayout>

    <!-- 3. Nút nổi (Floating Action Button) để thêm danh mục mới -->
    <!-- ID của nút này được giữ nguyên -->
    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/btn_add_category"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:backgroundTint="@color/ueh_orange"
        android:text="Thêm Danh Mục"
        android:textColor="@color/ueh_white"
        app:iconTint="@color/ueh_white" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
