<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- SỬA 1: Thẻ application cần có các thuộc tính và được đóng đúng cách -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UehNews"
        tools:targetApi="31">

        <!-- MainActivity là màn hình khởi chạy -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity là màn hình phụ -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false" />

        <!-- ... các activity khác ... -->

        <activity
            android:name=".ui.admin.AdminDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".ui.author.AuthorDashboardActivity"
            android:exported="false" />

    </application>

</manifest> <!-- SỬA 3: Thẻ manifest phải đóng ngoài cùng -->
